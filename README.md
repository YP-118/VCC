Add data folder, unzip competition set directly into data.

Output saved to outputs/models (.pth files)
and outputs/predictions (.h5ad files)

```bash
python -m scripts.train_model
```
```bash
python -m scripts.generate_submission
```
